Repository branch for github pages site.

Note that this branch is not to be edited directly, but updated from main when new documentation is generated.

The syntax for this is: git subtree murge --squash --prefix _build/html -b gh-pages

